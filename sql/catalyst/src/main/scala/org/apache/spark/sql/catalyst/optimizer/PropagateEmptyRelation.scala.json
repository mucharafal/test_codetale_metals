[{
  "comments": [{
    "author": {
      "login": "gatorsmile"
    },
    "body": "Nit: `An aggregate with non-empty group expression will return one output row per group.` \r\n-> \r\n`An aggregate with non-empty group expression will return one output row per group when the input to the aggregate is not empty.`",
    "commit": "fe8fe9a79f6a0228a08298c60942d6becfe20338",
    "createdAt": "2017-05-10T03:21:48Z",
    "diffHunk": "@@ -68,8 +63,13 @@ object PropagateEmptyRelation extends Rule[LogicalPlan] with PredicateHelper {\n       case _: LocalLimit => empty(p)\n       case _: Repartition => empty(p)\n       case _: RepartitionByExpression => empty(p)\n-      // AggregateExpressions like COUNT(*) return their results like 0.\n-      case Aggregate(_, ae, _) if !ae.exists(containsAggregateExpression) => empty(p)\n+      // An aggregate with non-empty group expression will return one output row per group."
  }],
  "prId": 17929
}]