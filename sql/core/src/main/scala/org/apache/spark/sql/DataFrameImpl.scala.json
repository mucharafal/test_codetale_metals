[{
  "comments": [{
    "author": {
      "login": "marmbrus"
    },
    "body": "Does this run a job?  Why aren't you just calling `collect()` so that it goes through the `executeCollect()` code path?\n",
    "commit": "552aa583c817563fde40d0cde4300b7dc8c92584",
    "createdAt": "2015-02-10T18:49:06Z",
    "diffHunk": "@@ -112,6 +112,12 @@ private[sql] class DataFrameImpl protected[sql](\n \n   override def printSchema(): Unit = println(schema.treeString)\n \n+  override def printPlan(extended: Boolean): Unit = {\n+    ExplainCommand(\n+      logicalPlan,\n+      extended = extended).rdd.collect().map(r => println(r.getString(0)))"
  }, {
    "author": {
      "login": "chenghao-intel"
    },
    "body": "Yeah, you're right, a little bit tricky thing here is `collect` is the API of `TreeNode`, and `ExplanCommand` is also a `TreeNode`, so it will causes ambiguous when implicitly convert to `DataFrame`, anyway, I will do that explicitly.\n",
    "commit": "552aa583c817563fde40d0cde4300b7dc8c92584",
    "createdAt": "2015-02-11T01:33:55Z",
    "diffHunk": "@@ -112,6 +112,12 @@ private[sql] class DataFrameImpl protected[sql](\n \n   override def printSchema(): Unit = println(schema.treeString)\n \n+  override def printPlan(extended: Boolean): Unit = {\n+    ExplainCommand(\n+      logicalPlan,\n+      extended = extended).rdd.collect().map(r => println(r.getString(0)))"
  }],
  "prId": 4496
}]