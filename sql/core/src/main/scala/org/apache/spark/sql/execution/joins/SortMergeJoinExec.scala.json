[{
  "comments": [{
    "author": {
      "login": "cloud-fan"
    },
    "body": "I think this can be done in a followup. For the first version we can just add a `HiveHashPartitioning`, which can satisfy `ClusteredDistribution`(save shuffle for aggregate) but not `HashClusteredDistribution`(can't save shuffle for join).",
    "commit": "d37eb8b3359981756c923948fe12833a56b61865",
    "createdAt": "2018-04-25T06:13:12Z",
    "diffHunk": "@@ -39,7 +40,10 @@ case class SortMergeJoinExec(\n     joinType: JoinType,\n     condition: Option[Expression],\n     left: SparkPlan,\n-    right: SparkPlan) extends BinaryExecNode with CodegenSupport {\n+    right: SparkPlan,\n+    requiredNumPartitions: Option[Int] = None,\n+    hashingFunctionClass: Class[_ <: HashExpression[Int]] = classOf[Murmur3Hash])",
    "line": 15
  }],
  "prId": 19001
}]