[{
  "comments": [{
    "author": {
      "login": "tdas"
    },
    "body": "why this change?\n",
    "commit": "45110370fb1889f244a6750ef2a18dbc9f1ba9c2",
    "createdAt": "2016-06-23T22:09:05Z",
    "diffHunk": "@@ -40,7 +40,7 @@ private[sql] class ParquetOptions(\n     if (!shortParquetCompressionCodecNames.contains(codecName)) {\n       val availableCodecs = shortParquetCompressionCodecNames.keys.map(_.toLowerCase)\n       throw new IllegalArgumentException(s\"Codec [$codecName] \" +\n-        s\"is not available. Available codecs are ${availableCodecs.mkString(\", \")}.\")\n+        s\"is not available. Known codecs are ${availableCodecs.mkString(\", \")}.\")",
    "line": 5
  }, {
    "author": {
      "login": "gatorsmile"
    },
    "body": "Just to make it consistent with the output of the other cases. See the code: https://github.com/apache/spark/blob/d6dc12ef0146ae409834c78737c116050961f350/sql/core/src/main/scala/org/apache/spark/sql/execution/datasources/CompressionCodecs.scala#L49-L51\n",
    "commit": "45110370fb1889f244a6750ef2a18dbc9f1ba9c2",
    "createdAt": "2016-06-24T01:11:17Z",
    "diffHunk": "@@ -40,7 +40,7 @@ private[sql] class ParquetOptions(\n     if (!shortParquetCompressionCodecNames.contains(codecName)) {\n       val availableCodecs = shortParquetCompressionCodecNames.keys.map(_.toLowerCase)\n       throw new IllegalArgumentException(s\"Codec [$codecName] \" +\n-        s\"is not available. Available codecs are ${availableCodecs.mkString(\", \")}.\")\n+        s\"is not available. Known codecs are ${availableCodecs.mkString(\", \")}.\")",
    "line": 5
  }, {
    "author": {
      "login": "HyukjinKwon"
    },
    "body": "`Available` was intentionally used because Parquet only supports snappy, gzip or lzo whereas `Known` was used for text-based ones (Please see https://github.com/apache/spark/pull/10805#r50206079) as they support compression codecs including other codecs but that lists the known ones.\n",
    "commit": "45110370fb1889f244a6750ef2a18dbc9f1ba9c2",
    "createdAt": "2016-11-14T01:34:14Z",
    "diffHunk": "@@ -40,7 +40,7 @@ private[sql] class ParquetOptions(\n     if (!shortParquetCompressionCodecNames.contains(codecName)) {\n       val availableCodecs = shortParquetCompressionCodecNames.keys.map(_.toLowerCase)\n       throw new IllegalArgumentException(s\"Codec [$codecName] \" +\n-        s\"is not available. Available codecs are ${availableCodecs.mkString(\", \")}.\")\n+        s\"is not available. Known codecs are ${availableCodecs.mkString(\", \")}.\")",
    "line": 5
  }],
  "prId": 13837
}]