[{
  "comments": [{
    "author": {
      "login": "holdenk"
    },
    "body": "Same comment as before (although if it doesn't make sense to change the accept urls maybe we could put this in a shared function).\n",
    "commit": "996dc69bb6ec31d3d2d1e65ab7ec2abf238ac882",
    "createdAt": "2015-09-17T00:48:15Z",
    "diffHunk": "@@ -151,4 +152,36 @@ class JDBCWriteSuite extends SharedSQLContext with BeforeAndAfter {\n     assert(2 === sqlContext.read.jdbc(url1, \"TEST.PEOPLE1\", properties).count)\n     assert(2 === sqlContext.read.jdbc(url1, \"TEST.PEOPLE1\", properties).collect()(0).length)\n   }\n+\n+  test(\"INSERT to JDBC Datasource with Unserializable Driver Properties\") {\n+\n+    object UnserializableH2Driver extends org.h2.Driver {\n+\n+      override def connect(url: String, info: Properties): Connection = {\n+\n+        val result = super.connect(url, info)\n+        info.put(\"unserializableDriver\", this)\n+        result\n+      }\n+\n+      override def getParentLogger: Logger = ???\n+    }\n+\n+    import scala.collection.JavaConversions._\n+\n+    val oldDrivers = DriverManager.getDrivers.filter(_.acceptsURL(\"jdbc:h2:\")).toSeq"
  }],
  "prId": 8785
}, {
  "comments": [{
    "author": {
      "login": "holdenk"
    },
    "body": "extra blank line\n",
    "commit": "996dc69bb6ec31d3d2d1e65ab7ec2abf238ac882",
    "createdAt": "2015-09-18T18:37:01Z",
    "diffHunk": "@@ -151,4 +150,13 @@ class JDBCWriteSuite extends SharedSQLContext with BeforeAndAfter {\n     assert(2 === sqlContext.read.jdbc(url1, \"TEST.PEOPLE1\", properties).count)\n     assert(2 === sqlContext.read.jdbc(url1, \"TEST.PEOPLE1\", properties).collect()(0).length)\n   }\n+\n+  test(\"INSERT to JDBC Datasource with Unserializable Driver Properties\") {\n+"
  }],
  "prId": 8785
}, {
  "comments": [{
    "author": {
      "login": "srowen"
    },
    "body": "Nit: most of the import changes in this PR are actually the wrong way; can you restore the ordering?\n",
    "commit": "996dc69bb6ec31d3d2d1e65ab7ec2abf238ac882",
    "createdAt": "2015-11-30T09:37:49Z",
    "diffHunk": "@@ -20,12 +20,11 @@ package org.apache.spark.sql.jdbc\n import java.sql.DriverManager\n import java.util.Properties\n \n-import org.scalatest.BeforeAndAfter\n-\n-import org.apache.spark.sql.{Row, SaveMode}\n import org.apache.spark.sql.test.SharedSQLContext\n import org.apache.spark.sql.types._\n+import org.apache.spark.sql.{Row, SaveMode}\n import org.apache.spark.util.Utils\n+import org.scalatest.BeforeAndAfter"
  }],
  "prId": 8785
}]