[{
  "comments": [{
    "author": {
      "login": "gatorsmile"
    },
    "body": "trigger the test and see whether it passes in 2.4?",
    "commit": "17e0e2a7c161f7192993367bb408a339e764c3b5",
    "createdAt": "2019-06-25T21:13:59Z",
    "diffHunk": "@@ -734,4 +734,54 @@ abstract class BucketedReadSuite extends QueryTest with SQLTestUtils {\n         df1.groupBy(\"j\").agg(max(\"k\")))\n     }\n   }\n+\n+  //  A test with a partition where the number of files in the partition is\n+  //  large. tests for the condition where the serialization of such a task may result in a stack\n+  //  overflow if the files list is stored in a recursive data structure\n+  //  This test is ignored because it takes long to run (~3 min)\n+  ignore(\"SPARK-27100 stack overflow: read data with large partitions\") {",
    "line": 9
  }, {
    "author": {
      "login": "dbtsai"
    },
    "body": "@parthchandra can you confirm this? Thanks.",
    "commit": "17e0e2a7c161f7192993367bb408a339e764c3b5",
    "createdAt": "2019-06-25T22:30:41Z",
    "diffHunk": "@@ -734,4 +734,54 @@ abstract class BucketedReadSuite extends QueryTest with SQLTestUtils {\n         df1.groupBy(\"j\").agg(max(\"k\")))\n     }\n   }\n+\n+  //  A test with a partition where the number of files in the partition is\n+  //  large. tests for the condition where the serialization of such a task may result in a stack\n+  //  overflow if the files list is stored in a recursive data structure\n+  //  This test is ignored because it takes long to run (~3 min)\n+  ignore(\"SPARK-27100 stack overflow: read data with large partitions\") {",
    "line": 9
  }, {
    "author": {
      "login": "parthchandra"
    },
    "body": "Yes. The test passes in 2.4 for both bucketing with/without hive.",
    "commit": "17e0e2a7c161f7192993367bb408a339e764c3b5",
    "createdAt": "2019-06-25T22:38:08Z",
    "diffHunk": "@@ -734,4 +734,54 @@ abstract class BucketedReadSuite extends QueryTest with SQLTestUtils {\n         df1.groupBy(\"j\").agg(max(\"k\")))\n     }\n   }\n+\n+  //  A test with a partition where the number of files in the partition is\n+  //  large. tests for the condition where the serialization of such a task may result in a stack\n+  //  overflow if the files list is stored in a recursive data structure\n+  //  This test is ignored because it takes long to run (~3 min)\n+  ignore(\"SPARK-27100 stack overflow: read data with large partitions\") {",
    "line": 9
  }],
  "prId": 24957
}]