[{
  "comments": [{
    "author": {
      "login": "srowen"
    },
    "body": "Can the flatMap become flatten? I may be missing why\n",
    "commit": "2567ef0b093079170635a5e2df0b98c0c831be84",
    "createdAt": "2016-02-05T11:00:54Z",
    "diffHunk": "@@ -215,8 +216,8 @@ object MasterFailureTest extends Logging {\n \n     while(!isLastOutputGenerated && !isTimedOut) {\n       // Get the output buffer\n-      val outputBuffer = ssc.graph.getOutputStreams().head.asInstanceOf[TestOutputStream[T]].output\n-      def output = outputBuffer.flatMap(x => x)\n+      val outputQueue = ssc.graph.getOutputStreams().head.asInstanceOf[TestOutputStream[T]].output\n+      def output = outputQueue.asScala.flatMap(x => x)"
  }, {
    "author": {
      "login": "holdenk"
    },
    "body": "for sure, I just wanted to try and make the changes as small as possible. I'll update this.\n",
    "commit": "2567ef0b093079170635a5e2df0b98c0c831be84",
    "createdAt": "2016-02-05T19:16:25Z",
    "diffHunk": "@@ -215,8 +216,8 @@ object MasterFailureTest extends Logging {\n \n     while(!isLastOutputGenerated && !isTimedOut) {\n       // Get the output buffer\n-      val outputBuffer = ssc.graph.getOutputStreams().head.asInstanceOf[TestOutputStream[T]].output\n-      def output = outputBuffer.flatMap(x => x)\n+      val outputQueue = ssc.graph.getOutputStreams().head.asInstanceOf[TestOutputStream[T]].output\n+      def output = outputQueue.asScala.flatMap(x => x)"
  }],
  "prId": 11067
}]