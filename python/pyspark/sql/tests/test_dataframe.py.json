[{
  "comments": [{
    "author": {
      "login": "HyukjinKwon"
    },
    "body": "@DylanGuedes, what do we get if `dict` is given? looks not being tested. If that produces a weird result, we can disallow it here for now.",
    "commit": "00a4888a64017fc08626ec1f0ca193caec5afa53",
    "createdAt": "2018-11-15T00:57:28Z",
    "diffHunk": "@@ -375,6 +375,19 @@ def test_generic_hints(self):\n         plan = df1.join(df2.hint(\"broadcast\"), \"id\")._jdf.queryExecution().executedPlan()\n         self.assertEqual(1, plan.toString().count(\"BroadcastHashJoin\"))\n \n+    # add tests for SPARK-23647 (test more types for hint)\n+    def test_extended_hint_types(self):\n+        from pyspark.sql import DataFrame\n+\n+        df = self.spark.range(10e10).toDF(\"id\")\n+        such_a_nice_list = [\"itworks1\", \"itworks2\", \"itworks3\"]",
    "line": 9
  }, {
    "author": {
      "login": "DylanGuedes"
    },
    "body": "@HyukjinKwon I tested locally and you are right, it is kinda weird, so I just removed it.",
    "commit": "00a4888a64017fc08626ec1f0ca193caec5afa53",
    "createdAt": "2018-11-20T21:53:46Z",
    "diffHunk": "@@ -375,6 +375,19 @@ def test_generic_hints(self):\n         plan = df1.join(df2.hint(\"broadcast\"), \"id\")._jdf.queryExecution().executedPlan()\n         self.assertEqual(1, plan.toString().count(\"BroadcastHashJoin\"))\n \n+    # add tests for SPARK-23647 (test more types for hint)\n+    def test_extended_hint_types(self):\n+        from pyspark.sql import DataFrame\n+\n+        df = self.spark.range(10e10).toDF(\"id\")\n+        such_a_nice_list = [\"itworks1\", \"itworks2\", \"itworks3\"]",
    "line": 9
  }],
  "prId": 20788
}]