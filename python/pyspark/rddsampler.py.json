[{
  "comments": [{
    "author": {
      "login": "mengxr"
    },
    "body": "There is an issue with the name here. Maybe we should keep the name `fraction` and rename `lowbound` to `acceptanceRangeStart`. Then check `acceptanceRangeStart + fraction <= 1.0 + eps` in the constructor, and call `RDDSampler(False, ub - lb, seed, lb).func` in `randomSplit`.\n",
    "commit": "78bf997f13c6f08129671a9d6a3484620d5b37a2",
    "createdAt": "2014-11-13T20:03:33Z",
    "diffHunk": "@@ -111,7 +112,7 @@ def func(self, split, iterator):\n                     yield obj\n         else:\n             for obj in iterator:\n-                if self.getUniformSample(split) <= self._fraction:\n+                if self._lowbound <= self.getUniformSample(split) < self._fraction:"
  }],
  "prId": 3193
}]