[{
  "comments": [{
    "author": {
      "login": "shivaram"
    },
    "body": "Could you add a note that this is deprecated here ?\n\nAlso this is the title of the document, so its better to have this be something like `Gets the number of partitions of an RDD` cc @felixcheung\n",
    "commit": "c17ce805504a89fc465f3fd1b9bce98dcda67a9a",
    "createdAt": "2015-12-04T01:07:05Z",
    "diffHunk": "@@ -306,17 +306,26 @@ setMethod(\"checkpoint\",\n #'\\dontrun{\n #' sc <- sparkR.init()\n #' rdd <- parallelize(sc, 1:10, 2L)\n-#' numPartitions(rdd)  # 2L\n+#' getNumPartitions(rdd)  # 2L\n #'}\n-#' @rdname numPartitions\n+#' @rdname getNumPartitions\n+#' @aliases getNumPartitions,RDD-method\n+#' @noRd\n+setMethod(\"getNumPartitions\",\n+          signature(x = \"RDD\"),\n+          function(x) {\n+            callJMethod(getJRDD(x), \"getNumPartitions\")\n+          })\n+\n+#' numPartitions is the same as getNumPartitions"
  }, {
    "author": {
      "login": "felixcheung"
    },
    "body": "It is, although we are not generating doc for internal API via `@noRd` below...\nStill, better to be descriptive.\n",
    "commit": "c17ce805504a89fc465f3fd1b9bce98dcda67a9a",
    "createdAt": "2015-12-04T01:55:34Z",
    "diffHunk": "@@ -306,17 +306,26 @@ setMethod(\"checkpoint\",\n #'\\dontrun{\n #' sc <- sparkR.init()\n #' rdd <- parallelize(sc, 1:10, 2L)\n-#' numPartitions(rdd)  # 2L\n+#' getNumPartitions(rdd)  # 2L\n #'}\n-#' @rdname numPartitions\n+#' @rdname getNumPartitions\n+#' @aliases getNumPartitions,RDD-method\n+#' @noRd\n+setMethod(\"getNumPartitions\",\n+          signature(x = \"RDD\"),\n+          function(x) {\n+            callJMethod(getJRDD(x), \"getNumPartitions\")\n+          })\n+\n+#' numPartitions is the same as getNumPartitions"
  }, {
    "author": {
      "login": "felixcheung"
    },
    "body": "In fact, if we are deprecating, consider adding\n\n```\n.Deprecated(newFuncSig, old = oldFuncSig)\n```\n\ncall to the deprecated version\n",
    "commit": "c17ce805504a89fc465f3fd1b9bce98dcda67a9a",
    "createdAt": "2015-12-04T01:57:39Z",
    "diffHunk": "@@ -306,17 +306,26 @@ setMethod(\"checkpoint\",\n #'\\dontrun{\n #' sc <- sparkR.init()\n #' rdd <- parallelize(sc, 1:10, 2L)\n-#' numPartitions(rdd)  # 2L\n+#' getNumPartitions(rdd)  # 2L\n #'}\n-#' @rdname numPartitions\n+#' @rdname getNumPartitions\n+#' @aliases getNumPartitions,RDD-method\n+#' @noRd\n+setMethod(\"getNumPartitions\",\n+          signature(x = \"RDD\"),\n+          function(x) {\n+            callJMethod(getJRDD(x), \"getNumPartitions\")\n+          })\n+\n+#' numPartitions is the same as getNumPartitions"
  }],
  "prId": 10123
}]