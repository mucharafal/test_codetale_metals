[{
  "comments": [{
    "author": {
      "login": "zsxwing"
    },
    "body": "Need to use `createStream[String]` to make the compiler happy. So it breaks the source compatibility. There are two `createStream` with default parameters and looks the compiler is confused. Maybe we should use a new method name for the new `createStream` with `actorSystemCreator`.\n",
    "commit": "8bd3e3dc1c859dd3b02348be1cfc716d7b7ce3c7",
    "createdAt": "2015-07-27T15:08:10Z",
    "diffHunk": "@@ -90,7 +89,9 @@ object ZeroMQWordCount {\n     def bytesToStringIterator(x: Seq[ByteString]): Iterator[String] = x.map(_.utf8String).iterator\n \n     // For this stream, a zeroMQ publisher should be running.\n-    val lines = ZeroMQUtils.createStream(ssc, url, Subscribe(topic), bytesToStringIterator _)\n+    val lines = ZeroMQUtils.createStream[String](",
    "line": 44
  }],
  "prId": 6841
}]