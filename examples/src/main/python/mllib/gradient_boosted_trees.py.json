[{
  "comments": [{
    "author": {
      "login": "mengxr"
    },
    "body": "Why using `v_p1` instead of `vp` or `v_p`?\n",
    "commit": "d7d63237036bec439700085ed31b6225199b38c1",
    "createdAt": "2015-04-16T04:41:17Z",
    "diffHunk": "@@ -49,8 +50,8 @@ def testRegression(trainingData, testData):\n     # Evaluate model on test instances and compute test error\n     predictions = model.predict(testData.map(lambda x: x.features))\n     labelsAndPredictions = testData.map(lambda lp: lp.label).zip(predictions)\n-    testMSE = labelsAndPredictions.map(lambda (v, p): (v - p) * (v - p)).sum() \\\n-        / float(testData.count())\n+    testMSE = labelsAndPredictions.map(lambda v_p1: (v_p1[0] - v_p1[1]) * (v_p1[0] - v_p1[1]))\\"
  }],
  "prId": 5173
}]