[{
  "comments": [{
    "author": {
      "login": "mengxr"
    },
    "body": "This should not happen inside `transform`. we cannot afford one `fit` call per `transform`. We should inside the label indexer into the `RFormula.fit`instead of `RFormulaModel.transform`.\n",
    "commit": "40b9a27361e422fd41ea0404cd61599e4afba00c",
    "createdAt": "2015-11-03T00:31:05Z",
    "diffHunk": "@@ -200,6 +200,13 @@ class RFormulaModel private[feature](\n       dataset.schema(labelName).dataType match {\n         case _: NumericType | BooleanType =>\n           dataset.withColumn($(labelCol), dataset(labelName).cast(DoubleType))\n+        case _: StringType => {\n+          val indexer = new StringIndexer()"
  }],
  "prId": 9302
}]