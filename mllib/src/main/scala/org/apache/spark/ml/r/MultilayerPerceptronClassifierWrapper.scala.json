[{
  "comments": [{
    "author": {
      "login": "felixcheung"
    },
    "body": "check length > 0 or check it in R?\n",
    "commit": "4524c863f2109d310af557c0c08886924e7b5a18",
    "createdAt": "2016-10-19T21:13:16Z",
    "diffHunk": "@@ -73,6 +75,8 @@ private[r] object MultilayerPerceptronClassifierWrapper\n       .setStepSize(stepSize)\n       .setPredictionCol(PREDICTED_LABEL_COL)\n     if (seed != null && seed.length > 0) mlp.setSeed(seed.toInt)\n+    if (initialWeights != null) mlp.setInitialWeights(Vectors.dense(initialWeights))"
  }, {
    "author": {
      "login": "WeichenXu123"
    },
    "body": "The length must be specific value, for example, \nif layer = [a, b, c],  weight length = a \\* (b + 1) + b \\* (c + 1)\nthis will be checked inside scala MLP class.\nso does r-side need recheck it ?\n",
    "commit": "4524c863f2109d310af557c0c08886924e7b5a18",
    "createdAt": "2016-10-20T01:27:33Z",
    "diffHunk": "@@ -73,6 +75,8 @@ private[r] object MultilayerPerceptronClassifierWrapper\n       .setStepSize(stepSize)\n       .setPredictionCol(PREDICTED_LABEL_COL)\n     if (seed != null && seed.length > 0) mlp.setSeed(seed.toInt)\n+    if (initialWeights != null) mlp.setInitialWeights(Vectors.dense(initialWeights))"
  }, {
    "author": {
      "login": "felixcheung"
    },
    "body": "that seems somewhat involved, so it is likely better to not duplicate the logic on the R side.\nwe should check here if initialWeights != null its length > 0\n",
    "commit": "4524c863f2109d310af557c0c08886924e7b5a18",
    "createdAt": "2016-10-20T04:11:28Z",
    "diffHunk": "@@ -73,6 +75,8 @@ private[r] object MultilayerPerceptronClassifierWrapper\n       .setStepSize(stepSize)\n       .setPredictionCol(PREDICTED_LABEL_COL)\n     if (seed != null && seed.length > 0) mlp.setSeed(seed.toInt)\n+    if (initialWeights != null) mlp.setInitialWeights(Vectors.dense(initialWeights))"
  }],
  "prId": 15552
}]