[{
  "comments": [{
    "author": {
      "login": "mengxr"
    },
    "body": "What is the difference between host and address? It is useful to mention it in the doc.",
    "commit": "c7600c24221d29fde31dca921d9d5863af2666e9",
    "createdAt": "2018-07-16T23:31:19Z",
    "diffHunk": "@@ -21,4 +21,8 @@ package org.apache.spark.scheduler\n  * Represents free resources available on an executor.\n  */\n private[spark]\n-case class WorkerOffer(executorId: String, host: String, cores: Int)\n+case class WorkerOffer(\n+    executorId: String,\n+    host: String,\n+    cores: Int,\n+    address: Option[String] = None)"
  }],
  "prId": 21758
}, {
  "comments": [{
    "author": {
      "login": "cloud-fan"
    },
    "body": "instead of having `host` and `address`, shall we just have `host` and `port`?",
    "commit": "c7600c24221d29fde31dca921d9d5863af2666e9",
    "createdAt": "2018-07-19T07:07:21Z",
    "diffHunk": "@@ -21,4 +21,10 @@ package org.apache.spark.scheduler\n  * Represents free resources available on an executor.\n  */\n private[spark]\n-case class WorkerOffer(executorId: String, host: String, cores: Int)\n+case class WorkerOffer(\n+    executorId: String,\n+    host: String,\n+    cores: Int,\n+    // `address` is an optional hostPort string, it provide more useful information than `host`\n+    // when multiple executors are launched on the same host.\n+    address: Option[String] = None)",
    "line": 11
  }, {
    "author": {
      "login": "jiangxb1987"
    },
    "body": "This follows what `ExecutorData` does, having both `executorAddress` and `executorHost`, IIUC the host of `executorAddress` doesn't necessarily have to be the same as `executorHost`.",
    "commit": "c7600c24221d29fde31dca921d9d5863af2666e9",
    "createdAt": "2018-07-19T14:11:59Z",
    "diffHunk": "@@ -21,4 +21,10 @@ package org.apache.spark.scheduler\n  * Represents free resources available on an executor.\n  */\n private[spark]\n-case class WorkerOffer(executorId: String, host: String, cores: Int)\n+case class WorkerOffer(\n+    executorId: String,\n+    host: String,\n+    cores: Int,\n+    // `address` is an optional hostPort string, it provide more useful information than `host`\n+    // when multiple executors are launched on the same host.\n+    address: Option[String] = None)",
    "line": 11
  }],
  "prId": 21758
}]