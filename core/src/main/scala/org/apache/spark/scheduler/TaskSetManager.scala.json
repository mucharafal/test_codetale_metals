[{
  "comments": [{
    "author": {
      "login": "kayousterhout"
    },
    "body": "How about \r\n\r\n\"Task ${info.id} in stage ${taskSet.id} (TID $tid) failed, but the task will not be re-executed (either because the task failed with a shuffle data fetch failure, so the previous stage needs to be re-run, or because a different copy of the task has already succeeded)\"",
    "commit": "a2cfd6959ba670fdfe806880e216978d3c27b94d",
    "createdAt": "2017-02-06T06:24:29Z",
    "diffHunk": "@@ -799,7 +799,7 @@ private[spark] class TaskSetManager(\n     if (successful(index)) {\n       logInfo(\n         s\"Task ${info.id} in stage ${taskSet.id} (TID $tid) failed, \" +\n-        \"but another instance of the task has already succeeded, \" +\n+        \"but it is already marked as successful, \" +"
  }],
  "prId": 16738
}]