[{
  "comments": [{
    "author": {
      "login": "HyukjinKwon"
    },
    "body": "Up to my knowledge, `contains` on `Option` breaks Scala 2.10 build (see https://github.com/apache/spark/pull/15393). I guess we are going to drop the support but not yet.",
    "commit": "4788bbef279828d96feead227b4b450e96493d4c",
    "createdAt": "2017-03-28T14:13:42Z",
    "diffHunk": "@@ -35,7 +35,7 @@ private[ui] class StagesTab(parent: SparkUI) extends SparkUITab(parent, \"stages\"\n   attachPage(new StagePage(this))\n   attachPage(new PoolPage(this))\n \n-  def isFairScheduler: Boolean = progressListener.schedulingMode == Some(SchedulingMode.FAIR)\n+  def isFairScheduler: Boolean = progressListener.schedulingMode.contains(SchedulingMode.FAIR)"
  }, {
    "author": {
      "login": "dbolshak"
    },
    "body": "Thanks for that comment. I did not know that.",
    "commit": "4788bbef279828d96feead227b4b450e96493d4c",
    "createdAt": "2017-03-28T20:31:19Z",
    "diffHunk": "@@ -35,7 +35,7 @@ private[ui] class StagesTab(parent: SparkUI) extends SparkUITab(parent, \"stages\"\n   attachPage(new StagePage(this))\n   attachPage(new PoolPage(this))\n \n-  def isFairScheduler: Boolean = progressListener.schedulingMode == Some(SchedulingMode.FAIR)\n+  def isFairScheduler: Boolean = progressListener.schedulingMode.contains(SchedulingMode.FAIR)"
  }, {
    "author": {
      "login": "dbolshak"
    },
    "body": "BTW, this was reverted.",
    "commit": "4788bbef279828d96feead227b4b450e96493d4c",
    "createdAt": "2017-03-29T13:39:56Z",
    "diffHunk": "@@ -35,7 +35,7 @@ private[ui] class StagesTab(parent: SparkUI) extends SparkUITab(parent, \"stages\"\n   attachPage(new StagePage(this))\n   attachPage(new PoolPage(this))\n \n-  def isFairScheduler: Boolean = progressListener.schedulingMode == Some(SchedulingMode.FAIR)\n+  def isFairScheduler: Boolean = progressListener.schedulingMode.contains(SchedulingMode.FAIR)"
  }, {
    "author": {
      "login": "HyukjinKwon"
    },
    "body": "It seems not reverted yet..",
    "commit": "4788bbef279828d96feead227b4b450e96493d4c",
    "createdAt": "2017-03-30T09:01:19Z",
    "diffHunk": "@@ -35,7 +35,7 @@ private[ui] class StagesTab(parent: SparkUI) extends SparkUITab(parent, \"stages\"\n   attachPage(new StagePage(this))\n   attachPage(new PoolPage(this))\n \n-  def isFairScheduler: Boolean = progressListener.schedulingMode == Some(SchedulingMode.FAIR)\n+  def isFairScheduler: Boolean = progressListener.schedulingMode.contains(SchedulingMode.FAIR)"
  }],
  "prId": 17458
}]