[{
  "comments": [{
    "author": {
      "login": "andrewor14"
    },
    "body": "Do we actually ever want to start an external shuffle service in a local cluster? If not I think it makes more sense to just set `spark.shuffle.service.enabled` to false in `LocalSparkCluster` (we already do this for the REST submission server for Master)\n",
    "commit": "3c2d6edfeb02488e176703e712373f88c2ba1037",
    "createdAt": "2015-04-21T00:03:22Z",
    "diffHunk": "@@ -43,6 +43,9 @@ class ExternalShuffleServiceSuite extends ShuffleSuite with BeforeAndAfterAll {\n     conf.set(\"spark.shuffle.manager\", \"sort\")\n     conf.set(\"spark.shuffle.service.enabled\", \"true\")\n     conf.set(\"spark.shuffle.service.port\", server.getPort.toString)\n+\n+    // local-cluster mode starts a Worker which would start its own shuffle service without this:\n+    conf.set(\"spark.worker.shouldHostShuffleServiceIfEnabled\", \"false\")"
  }],
  "prId": 5575
}]